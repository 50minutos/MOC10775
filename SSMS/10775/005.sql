SELECT NAME, 
	LOG_REUSE_WAIT_DESC, 
	* 
FROM SYS.DATABASES

USE MASTER

DROP DATABASE DB

CREATE DATABASE DB
GO

USE DB

CREATE TABLE PESSOA
(
	COD_PESSOA INT IDENTITY PRIMARY KEY, 
	NOME_PESSOA VARCHAR(50), 
	OBS_PESSOA CHAR(4000)
)

BEGIN TRAN

DECLARE @X INT = 1

WHILE @X <= 100000
BEGIN
	INSERT PESSOA 
	VALUES (NEWID(), NEWID())

	SET @X += 1
END

COMMIT

DBCC SHRINKDATABASE('DB')


